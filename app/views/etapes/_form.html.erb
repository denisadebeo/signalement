<%= form_with(model: etape) do |form| %>
  <% if etape.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(etape.errors.count, "error") %> prohibited this etape from being saved:</h2>

      <ul>
        <% etape.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :dossier_id, style: "display: block" %>
    <%= form.text_field :dossier_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :commentaire, style: "display: block" %>
    <%= form.text_area :commentaire %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
